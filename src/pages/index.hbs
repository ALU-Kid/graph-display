<!DOCTYPE html>
<html>
<head>
  <title>RageGraph Dashboard</title>
  <style>
    body { font-family: sans-serif; margin: 2rem; background: #111; color: #fff; }
    h1 { color: #00ffae; }
    .card { background: #222; padding: 1rem; margin-bottom: 1rem; border-radius: 10px; }
    input, button { padding: 0.5rem; border-radius: 5px; }
  </style>
</head>
<body>

  <h1>⚡ RageGraph Control Panel</h1>

  <div class="card">
    <h2>📝 Add a Custom Message</h2>
    <form method="POST" action="/add">
      <input type="text" name="newMessage" placeholder="Enter message like 'Stay Charged 🔋'" />
      <button type="submit">Add to Queue</button>
    </form>
  </div>

  <div class="card">
    <h2>📦 Upcoming Messages</h2>
    <ul>
      {{#each queue}}
        <li>{{this}}</li>
      {{/each}}
    </ul>
  </div>

  <div class="card">
    <h2>📊 Current Stats</h2>
    <p><strong>Total kWh this week:</strong> {{stats.kwh}} kWh</p>
    <p><strong>Sessions:</strong> {{stats.sessions}}</p>
    <p><strong>Weather:</strong> {{stats.weather}}</p>
  </div>

  <div class="card">
    <h2>⏩ Next Message to Render:</h2>
    <p><code>{{nextMessage}}</code></p>
  </div>

</body>
</html>
